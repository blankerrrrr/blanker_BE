## API 테스트 결과

### 요약

- 현재 구현에서 `/api/interest-item-groups` API는 제거됨
- 중복 그룹 테이블과 API는 `20260714_0012` 마이그레이션 이후 사용하지 않음

### 1. 기존 GET /api/interest-item-groups/{interestItemGroupId}

- 현재 구현 대상 아님

### 2. 기존 POST /api/interest-item-groups/{interestItemGroupId}/sources

- 현재 구현 대상 아님

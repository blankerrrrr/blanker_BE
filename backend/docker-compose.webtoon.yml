services:
  webtoon-api:
    build:
      context: ./webtoon-api
    expose:
      - "3000"
    ports:
      - "3000:3000"
    environment:
      NODE_ENV: development
      PORT: 3000
    networks:
      - blanker-network

networks:
  blanker-network:
    name: blanker-network
    external: true
